# Interseguro Coding Challenge

Two REST APIs that talk to each other over HTTP. go-api (Go, Fiber) rotates a matrix, computes its QR factorization, and sends the result to node-api (Node, Express), which computes statistics over the data and returns them.

Built with hexagonal architecture, JWT authentication, Docker, and full test coverage.
